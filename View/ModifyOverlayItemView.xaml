<UserControl x:Class="CatboobGGStream.View.ModifyOverlayItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CatboobGGStream.View"
             mc:Ignorable="d" 
             Visibility="{Binding ModifyOverlayItemVisibility}"
             d:DesignHeight="450" d:DesignWidth="300">

    <!-- Import the styles to apply to control elements.-->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ResourceDictionary/MaterialDesignResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Modify Toolbar -->
        <local:ToolBarView x:Name="ModifyOverlayItemToolbar" 
                           Grid.Row="0" Grid.Column="0"
                           Loaded="ModifyOverlayItemToolbar_Loaded"/>

        <ScrollViewer x:Name="ModifyOverlayItemScrollViewer" 
                      Grid.Row="1" Grid.Column="0"
                      VerticalScrollBarVisibility="Hidden" 
                      HorizontalContentAlignment="Stretch" 
                      VerticalContentAlignment="Stretch" 
                      ClipToBounds="True">

            <StackPanel>

                <!-- Image Path -->
                <Grid Margin="0,16,0,0">
                    
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="GetImagePathLabel" 
                               Grid.Row="0" Grid.Column="0" 
                               Margin="16,0" Padding="0" 
                               FontSize="14" 
                               VerticalAlignment="Center" 
                               Foreground="#FF212121" 
                               Text="Image Path:" />

                    <Button x:Name="GetImagePathButton"
                            Grid.Row="0" Grid.Column="1" 
                            Margin="0" Padding="16,0" 
                            Style="{StaticResource RegularBtn_style}" 
                            OverridesDefaultStyle="True" 
                            BorderThickness="0"
                            Click="GetImagePathButton_Click">
                        <!--Command="{Binding ImagePathClick}" >-->
                                               
                        <Image Width="24" Height="24" 
                               Source="../img/AttachmentGrey.png"/>
                    </Button>

                </Grid>

                <TextBlock x:Name="ImagePathTextBox" 
                           Margin="16,0" Padding="0" 
                           FontSize="14" 
                           Background="{x:Null}" 
                           Text="{Binding CurrentOverlayItemModel.ImagePath, Mode=TwoWay}" />

                <!-- Item Seperator -->
                <Border BorderThickness="0,0,0,1" BorderBrush="#FFB6B6B6" Margin="0,8,0,0" />

                <!-- Hotkey -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="GetHotkeyLabel" 
                               Grid.Row="0" Grid.Column="0"  
                               Margin="16,0" Padding="0"
                               FontSize="14" 
                               VerticalAlignment="Center" 
                               Text="Hotkey:" 
                               Foreground="#FF212121" />

                    <Button x:Name="GetHotkeyButton"
                            Grid.Row="0" Grid.Column="1" 
                            Style="{StaticResource RegularBtn_style}" 
                            OverridesDefaultStyle="True" 
                            BorderThickness="0" 
                            Margin="0" Padding="16,0" 
                            Command="{Binding HotkeyClick}" >
                        <Image Width="24" Height="24" 
                               Source="../img/KeyboardGrey.png"/>
                    </Button>

                </Grid>

                <TextBlock x:Name="HotkeyTextBox" 
                           Margin="16,0" Padding="0" 
                           FontSize="14" 
                           Background="{x:Null}" 
                           Text="{Binding CurrentOverlayItemModel.HotKey, Mode=TwoWay}" />

                <!-- Item Seperator -->
                <Border BorderThickness="0,0,0,1" BorderBrush="#FFB6B6B6" Margin="0,8" />

                <!-- Sound Path -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="GetSoundPathLabel" 
                               Grid.Row="0" Grid.Column="0"  
                               Margin="16,0" Padding="0" 
                               FontSize="14" 
                               VerticalAlignment="Center" 
                               Text="Sound Path:" 
                               Foreground="#FF212121" />

                    <Button x:Name="GetSoundPathButton" 
                            Grid.Column="1" 
                            Margin="0" Padding="16,0" 
                            Style="{StaticResource RegularBtn_style}" 
                            OverridesDefaultStyle="True" 
                            BorderThickness="0"
                            Click="GetSoundPathButton_Click">
                        <Image Width="24" Height="24" 
                               Source="../img/MusicNodeGrey.png"/>
                    </Button>

                </Grid>

                <TextBlock x:Name="SoundPathTextBox" 
                           Margin="16,0" Padding="0" 
                           FontSize="14" 
                           Background="{x:Null}" 
                           Text="{Binding CurrentOverlayItemModel.SoundPath, Mode=TwoWay}"/>

                <!-- Item Seperator -->
                <Border BorderThickness="0,0,0,1" BorderBrush="#FFB6B6B6" Margin="0,8" />

                <!-- Image Duration -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="GetDispalyDurationLabel"
                        Grid.Column="0" 
                               Margin="16,0" Padding="0"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Text="Display Duration:"
                               Foreground="#FF212121" />

                    <Button x:Name="GetDisplayDurationButton"
                            Grid.Column="1" 
                            Style="{StaticResource RegularBtn_style}" 
                            OverridesDefaultStyle="True" 
                            BorderThickness="0" 
                            Margin="0" Padding="16,0" 
                            Command="{Binding DisplayDurationClick}">
                        <Image Width="24" Height="24" 
                               Source="../img/TimePicker.png"/>
                    </Button>

                </Grid>

                <TextBlock x:Name="DurationTextBox" 
                           Margin="16,0" Padding="0" 
                           FontSize="14" 
                           Background="{x:Null}" 
                           Text="{Binding CurrentOverlayItemModel.DisplayDuration, Mode=TwoWay}"/>

                <!-- Item Seperator -->
                <Border BorderThickness="0,0,0,1" BorderBrush="#FFB6B6B6" Margin="0,8" />

                <!-- Image Fade In Duration -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="GetFadeInDurationLabel"
                        Grid.Column="0" 
                               Margin="16,0" Padding="0"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Text="Fade In Duration:"
                               Foreground="#FF212121" />

                    <Button x:Name="GetFadeInDurationButton"
                            Grid.Column="1" 
                            Style="{StaticResource RegularBtn_style}" 
                            OverridesDefaultStyle="True" 
                            BorderThickness="0" 
                            Margin="0" Padding="16,0" 
                            Command="{Binding FadeInDurationClick}">
                        <Image Width="24" Height="24" 
                               Source="../img/TimePicker.png"/>
                    </Button>

                </Grid>

                <TextBlock x:Name="FadeInDurationTextBox" 
                           Margin="16,0" Padding="0" 
                           FontSize="14" 
                           Background="{x:Null}" 
                           Text="{Binding CurrentOverlayItemModel.FadeInDuration, Mode=TwoWay}"/>

                <!-- Item Seperator -->
                <Border BorderThickness="0,0,0,1" BorderBrush="#FFB6B6B6" Margin="0,8" />

                <!-- Image Fade Out Duration -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="GetFadeOutDurationLabel"
                        Grid.Column="0" 
                               Margin="16,0" Padding="0"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Text="Fade Out Duration:"
                               Foreground="#FF212121" />

                    <Button x:Name="GetFadeOutDurationButton"
                            Grid.Column="1" 
                            Style="{StaticResource RegularBtn_style}" 
                            OverridesDefaultStyle="True" 
                            BorderThickness="0" 
                            Margin="0" Padding="16,0" 
                            Command="{Binding FadeOutDurationClick}">
                        <Image Width="24" Height="24" 
                               Source="../img/TimePicker.png"/>
                    </Button>

                </Grid>

                <TextBlock x:Name="FadeOutDurationTextBox" 
                           Margin="16,0" Padding="0" 
                           FontSize="14" 
                           Background="{x:Null}" 
                           Text="{Binding CurrentOverlayItemModel.FadeOutDuration, Mode=TwoWay}"/>

                <!-- Item Seperator -->
                <Border BorderThickness="0,0,0,1" BorderBrush="#FFB6B6B6" Margin="0,8" />

            </StackPanel>

        </ScrollViewer>
        
        <!-- Get Form Property Controls Go Here -->
        <local:TimePickerView x:Name="TimeDurationPicker"
                              Grid.Row="0" Grid.Column="0"
                              Grid.RowSpan="2"
                              Loaded="TimeDurationPicker_Loaded"/>

    </Grid>
    
</UserControl>
